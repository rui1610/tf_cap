// For format details, see https://aka.ms/devcontainer.json.
{
    "name": "Terraform provider for SAP BTP (dev!)",
    "image": "ghcr.io/rui1610/tf_cap-tf:main",
    "customizations": {},
    "remoteUser": "user",
    "postCreateCommand": "cd /home/user && echo 'export PATH=\"$HOME/.local/bin:$PATH\"' >> ~/.profile && source ~/.profile",
    "workspaceFolder": "/home/user",
    "hostRequirements": {
        "cpus": 2,
        "memory": "4gb",
        "storage": "32gb"
    }
}